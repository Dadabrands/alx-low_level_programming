#include <stdio.h>

/*
 * main - Entry point
 * Description: A program that print a line using use printf function
 * Return: Always 0 (Success)
 */

int main(void)
{
printf("With proper grammar, but the outcome is a piece of art,\n");
return (0);
}
